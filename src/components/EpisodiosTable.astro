---
import type { Version } from "@/types/enum.ts";

interface Thumbnail {
  url: string;
  width: number;
  height: number;
}

interface Thumbnails {
  default: Thumbnail;
  maxres: Thumbnail;
  medium: Thumbnail;
  high: Thumbnail;
  standard: Thumbnail;
}

interface Props {
  videoId: string;
  episodeTitle: string;
  episodeNumber: string;
  thumbnails: Thumbnails;
  version: Version;
}

const { videoId, episodeNumber, episodeTitle, thumbnails, version } =
  Astro["props"];
---

<table>
  <thead>
    <tr>
      <th>Thumbnail</th>
      <th>Nº Episodio</th>
      <th>Titulo Episodio</th>
      <th>Version</th>
      <th>Links</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <img src={thumbnails.medium.url} alt={episodeTitle} />
      </td>
      <td>
        <h3>{episodeNumber} - {episodeTitle}</h3>
      </td>
      <td>
        <p>{version}</p>
      </td>
      <td>
        <p>{version}</p>
      </td>
      <td>
        <a href={`https://www.youtube.com/watch?v=${videoId}`}>
          Ver en YouTube
        </a>
        <a href={`/episodios/${episodeTitle}`}> Ver Info </a>
      </td>
    </tr>
  </tbody>
</table>

<style>
  table {
    width: 100%;
    border-collapse: collapse;
  }

  th,
  td {
    border: 1px solid #ddd;
    padding: 8px;
    text-align: left;
  }

  th {
    background-color: #f2f2f2;
  }

  tr:nth-child(even) {
    background-color: #f2f2f2;
  }
</style>

<script></script>
